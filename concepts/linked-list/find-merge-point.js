function findMergePoint(head1, head2) {
  // 1. find the length of both the linked lists.
  // 2. find the difference between the length of the linked list.
  // 3. move the pointer of the larger list to the difference point.
  // 4. then move the pointers in both list equally to check the merge point.
  // return if we find the merge point else return null after the for loop.
}
